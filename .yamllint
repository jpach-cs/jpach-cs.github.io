# Matches the line-length rule in the umbrella AGENTS.md.
extends: default
rules:
  line-length:
    max: 120
  document-start: disable
  truthy:
    # GitHub Actions uses `on:` as a top-level key.
    check-keys: false
ignore: |
  node_modules/
  _site/
